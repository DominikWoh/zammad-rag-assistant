# Projektweite Environment-Variablen für docker compose
# Benennung: wird von docker-compose.yml (Service qdrant) verwendet.
# Kopiere diese Datei nach ".env" im Projektwurzelverzeichnis und passe bei Bedarf an.

# Optionaler API-Key für Qdrant (leer = kein API-Key erforderlich)
# Erzeuge bei Bedarf einen zufälligen Wert, z.B. Python: 
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
QDRANT_API_KEY=